import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CheckSavePage } from './check-save.page';

describe('CheckSavePage', () => {
  let component: CheckSavePage;
  let fixture: ComponentFixture<CheckSavePage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CheckSavePage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CheckSavePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
