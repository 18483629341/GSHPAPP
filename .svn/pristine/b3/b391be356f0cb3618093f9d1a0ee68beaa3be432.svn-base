import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ApprovalForm2Page } from './approval-form2.page';

describe('ApprovalForm2Page', () => {
  let component: ApprovalForm2Page;
  let fixture: ComponentFixture<ApprovalForm2Page>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ApprovalForm2Page ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ApprovalForm2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
