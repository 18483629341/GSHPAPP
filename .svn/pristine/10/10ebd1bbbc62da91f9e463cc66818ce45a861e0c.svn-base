import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ProjectFilesPage } from './project-files.page';

describe('ProjectFilesPage', () => {
  let component: ProjectFilesPage;
  let fixture: ComponentFixture<ProjectFilesPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ProjectFilesPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ProjectFilesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
