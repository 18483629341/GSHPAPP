import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { HpzjInfoPage } from './hpzj-info.page';

describe('HpzjInfoPage', () => {
  let component: HpzjInfoPage;
  let fixture: ComponentFixture<HpzjInfoPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ HpzjInfoPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(HpzjInfoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
