import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EstabunitInfoPage } from './estabunit-info.page';

describe('EstabunitInfoPage', () => {
  let component: EstabunitInfoPage;
  let fixture: ComponentFixture<EstabunitInfoPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EstabunitInfoPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EstabunitInfoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
